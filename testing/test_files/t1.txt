CREATE OR REPLACE PACKAGE BODY JOB_PKG AS
  PROCEDURE GET(PID IN INTEGER, PCURSOR OUT SYS_REFCURSOR)
  IS
  BEGIN
    OPEN PCURSOR FOR
      SELECT *
      FROM JOB
      WHERE ID=PID;
  END GET;
END JOB_PKG;